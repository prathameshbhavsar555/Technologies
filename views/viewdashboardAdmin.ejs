<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin Dashboard</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', sans-serif;
    }

    .top-bar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background-color: #eef8f0;
      padding: 12px 20px;
      border-radius: 10px;
      margin-bottom: 25px;
    }

    .top-bar h2 {
      font-size: 20px;
      font-weight: 600;
      color: #2c3e50;
    }

    .logout-btn {
      background: #ff4d30;
      color: white;
      padding: 8px 16px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-weight: bold;
    }

    .dashboard {
      display:flex;
      justify-content: space-around;
      gap: 20px;
    }
    

    .card {
      background: #ffffff;
      border-radius: 12px;
      padding: 20px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
    }

    .icon {
      width: 50px;
      height: 50px;
      background-color: #ff5722;
      color: white;
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 22px;
      margin-bottom: 15px;
    }

    .card h4 {
      font-size: 16px;
      color: #444;
      margin-bottom: 5px;
    }

    .card h3 {
      font-size: 24px;
      color: #111;
      margin-bottom: 10px;
    }

    .badge {
      display: inline-block;
      padding: 4px 10px;
      font-size: 12px;
      border-radius: 6px;
      background-color: #ddd;
      margin-right: 6px;
    }

    .badge.available {
      background-color: #28a745;
      color: white;
    }

    .badge.occupied {
      background-color: #6c757d;
      color: white;
    }

    .badge.completed {
      background-color: #007bff;
      color: white;
    }

    .badge.pending {
      background-color: #ffc107;
      color: black;
    }
  </style>
</head>
<body>

   <div class="top-bar">
    <h2>Welcome to <strong>Restaurant Dashboard</strong></h2>
  </div>
  <div class="dashboard">
    <!-- Total Categories -->
    <div class="card">
      <div class="icon"><i class="fas fa-bezier-curve"></i></div>
      <h4>Total Categories</h4>
      <h3><%= totalCategories %></h3>
    </div>

    <!-- Total Menus -->
    <div class="card">
      <div class="icon"><i class="fas fa-utensils"></i></div>
      <h4>Total Menus</h4>
      <h3><%= totalMenus %></h3>
    </div>

    <!-- Total Tables -->
    <div class="card">
      <div class="icon"><i class="fas fa-chair"></i></div>
      <h4>Total Tables</h4>
      <h3><%= totalTables %></h3>
      <div>
        <span class="badge available">Available: <%= availableTables %></span>
        <span class="badge occupied">Occupied: <%= occupiedTables %></span>
      </div>
    </div>

    <!-- Total Orders -->
    <div class="card">
      <div class="icon"><i class="fas fa-clipboard-list"></i></div>
      <h4>Total Orders</h4>
      <h3><%= totalOrders %></h3>
      <div>
        <span class="badge completed">Completed: <%= completedOrders %></span>
        <span class="badge pending">Pending: <%= pendingOrders %></span>
      </div>
    </div>
  </div>

</body>
</html>
