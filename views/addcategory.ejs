<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Register Category</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />

  <style>
    :root {
      --primary: #fc8019;
      --hover: #f95d00;
      --bg: #fdfdfd;
      --text-dark: #333;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    /* body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #f7f8fa;
      color: var(--text-dark);
    } */

    .container {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 75vh;
      padding: 20px;
    }

    .form-box {
      background: white;
      padding: 40px 30px;
      border-radius: 16px;
      box-shadow: 0 8px 28px rgba(0, 0, 0, 0.08);
      width: 100%;
      max-width: 420px;
    }

    .form-box h2 {
      text-align: center;
      color: var(--primary);
      margin-bottom: 25px;
      font-size: 1.9rem;
      font-weight: 600;
    }

    .form-box h2 i {
      margin-right: 8px;
    }

    label {
      display: block;
      margin-bottom: 8px;
      font-weight: 600;
    }

    input[type="text"] {
      width: 100%;
      padding: 12px 14px;
      border: 1px solid #ddd;
      border-radius: 10px;
      margin-bottom: 20px;
      font-size: 1rem;
      transition: border-color 0.3s ease;
    }

    input[type="text"]:focus {
      outline: none;
      border-color: var(--primary);
      box-shadow: 0 0 0 3px rgba(252, 128, 25, 0.1);
    }

    button {
      width: 100%;
      background-color: var(--primary);
      color: white;
      padding: 12px;
      border: none;
      border-radius: 10px;
      font-size: 1rem;
      font-weight: 600;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    button:hover {
      background-color: var(--hover);
    }

    button i {
      margin-right: 6px;
    }

    .message {
      text-align: center;
      margin-top: 15px;
      color: green;
      font-weight: 500;
    }

    @media (max-width: 480px) {
      .form-box {
        padding: 30px 20px;
      }

      .form-box h2 {
        font-size: 1.5rem;
      }

      input[type="text"], button {
        font-size: 0.95rem;
      }
    }
  </style>
</head>
<body>
  <section class="container">
    <div class="form-box">
      <h2><i class="fas fa-folder-plus"></i>Register Category</h2>
      <form action="/insertcategories" method="POST">
        <label for="name">Category Name</label>
        <input type="text" id="name" name="name" placeholder="e.g. Starters, Desserts" required>

        <button type="submit"><i class="fas fa-check-circle"></i>Register</button>
      </form>

      <% if (msg) { %>
        <div class="message"><%= msg %></div>
      <% } %>
    </div>
  </section>
</body>
</html>
