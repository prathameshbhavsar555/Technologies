<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>All Paid Bills - Swiggy Style</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    * {
      box-sizing: border-box;
      font-family: 'Segoe UI', sans-serif;
      margin: 0;
      padding: 0;
    }


    h2 {
      text-align: center;
      margin-bottom: 30px;
      color: #fc8019;
      font-size: 26px;
      font-weight: bold;
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
      gap: 20px;
      max-width: 1200px;
      margin: auto;
    }

    .bill-card {
      background-color: #fff;
      border-radius: 12px;
      padding: 20px 22px;
      box-shadow: 0 8px 18px rgba(0, 0, 0, 0.06);
      transition: transform 0.2s ease;
      border-top: 4px solid #fc8019;
    }

    .bill-card:hover {
      transform: translateY(-4px);
    }

    .bill-card h4 {
      margin-bottom: 12px;
      font-size: 18px;
      color: #222;
      border-bottom: 1px dashed #ddd;
      padding-bottom: 6px;
    }

    .bill-card p {
      font-size: 14.5px;
      margin: 6px 0;
      color: #555;
    }

    .bill-card p strong {
      color: #333;
    }

    @media (max-width: 600px) {
      h2 {
        font-size: 22px;
      }
      .bill-card {
        padding: 18px;
      }
    }
  </style>
</head>
<body>

  <h2>🍽️ All Paid Bills</h2>

  <div class="grid">
    <% bills.forEach(bill => { %>
      <div class="bill-card">
        <h4>Order ID: <%= bill.order_id %></h4>
        <p><strong>Table:</strong> <%= bill.table_id %></p>
        <p><strong>Staff:</strong> <%= bill.staff_name %></p>
        <p><strong>Date:</strong> <%= bill.ord_date %></p>
        <p><strong>Total:</strong> ₹<%= bill.total_amt %></p>
      </div>
    <% }) %>
  </div>

</body>
</html>
