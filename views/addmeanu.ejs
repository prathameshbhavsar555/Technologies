<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Add Menu Item</title>

  <!-- Fonts & Icons -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"/>

  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    .container22{
      font-family: 'Inter', sans-serif;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 20px;
    }

    .form-wrapper {
      background: rgba(255, 255, 255, 0.9);
      backdrop-filter: blur(8px);
      padding: 40px 30px;
      border-radius: 16px;
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.1);
      max-width: 500px;
      width: 100%;
      animation: fadeIn 0.6s ease-in-out;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(30px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    h2 {
      text-align: center;
      font-size: 28px;
      color: #fc8019;
      margin-bottom: 30px;
    }

    .form-group {
      margin-bottom: 22px;
    }

    .input-icon-wrapper {
      position: relative;
    }

    .input-icon-wrapper i {
      position: absolute;
      left: 12px;
      top: 50%;
      transform: translateY(-50%);
      color: #888;
    }

    .input-icon-wrapper input,
    .input-icon-wrapper select {
      width: 100%;
      padding: 12px 12px 12px 40px;
      border: 1.5px solid #ccc;
      border-radius: 10px;
      font-size: 15px;
      transition: border 0.3s ease;
      background-color: #fff;
    }

    .input-icon-wrapper input[type="file"] {
      padding-left: 12px;
    }

    .input-icon-wrapper input:focus,
    .input-icon-wrapper select:focus {
      border-color: #00bcd4;
      outline: none;
    }

    label {
      display: block;
      margin-bottom: 8px;
      font-weight: 600;
      color: #444;
    }

    input[type="submit"] {
      background: #fc8019;
      border: none;
      color: #fff;
      padding: 14px;
      font-size: 16px;
      border-radius: 10px;
      width: 100%;
      cursor: pointer;
      font-weight: 600;
      transition: background 0.3s ease, transform 0.2s ease;
    }

    input[type="submit"]:hover {
      background: #ac5004;
      transform: translateY(-1px);
    }

    .message {
      text-align: center;
      margin-top: 15px;
      color: #28a745;
      font-weight: 500;
    }

    @media (max-width: 600px) {
      .form-wrapper {
        padding: 30px 20px;
      }

      h2 {
        font-size: 24px;
      }
    }
  </style>
</head>
<body>
<section class="container22">
   <div class="form-wrapper">
    <h2><i class="fas fa-plus-circle"></i> Add Menu Item</h2>

    <form action="/addmenuInDB" method="POST" enctype="multipart/form-data">
      
      <div class="form-group">
        <label for="item_name">Item Name</label>
        <div class="input-icon-wrapper">
          <i class="fas fa-utensils"></i>
          <input type="text" id="item_name" name="item_name" placeholder="e.g. Paneer Butter Masala" required>
        </div>
      </div>

      <div class="form-group">
        <label for="category_id">Category</label>
        <div class="input-icon-wrapper">
          <i class="fas fa-layer-group"></i>
          <select name="category_id" id="category_id" required>
            <option value="" disabled selected>Select a category</option>
            <% data.forEach(item => { %>
              <option value="<%= item.id %>"><%= item.name %></option>
            <% }) %>
          </select>
        </div>
      </div>

      <div class="form-group">
        <label for="price">Price (₹)</label>
        <div class="input-icon-wrapper">
          <i class="fas fa-rupee-sign"></i>
          <input type="number" step="0.01" id="price" name="price" placeholder="e.g. 199.99" required>
        </div>
      </div>

      <div class="form-group">
        <label for="description">Description <small>(optional)</small></label>
        <div class="input-icon-wrapper">
          <i class="fas fa-align-left"></i>
          <input type="text" id="description" name="description" placeholder="Short description">
        </div>
      </div>

      <div class="form-group">
        <label for="image">Upload Image</label>
        <div class="input-icon-wrapper">
          <!-- <i class="fas fa-image"></i> -->
          <input type="file" id="image" name="image" accept="image/*" required>
        </div>
      </div>

      <div class="form-group">
        <input type="submit" value="Add Menu Item">
      </div>
    </form>

    <% if (typeof msg !== "undefined") { %>
      <div class="message"><%= msg %></div>
    <% } %>
  </div>
</section>
</body>
</html>
